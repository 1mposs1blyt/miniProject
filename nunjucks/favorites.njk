<!DOCTYPE html>
<html lang="en">

<head>
    {% include "templates/head.html"%}
</head>

<body>
    {% include "templates/header.njk" %}
    <main>
        <div class="container">
            <h1>Избранное</h1>
            <a>Товаров:{{CardTovarQuantity}} 4 на сумму: {{FavoriteTovarSum}}10 500{{item.currency}}</a>
            <p><input type="checkbox">Выбрать все<a role="button" onclick="CartClean()"> Удалить выбранные</a>
            </p>

            <div class="row">

                {% for item in CartItems %}
                <div class="card mb-3" style="max-width: 1200px;">
                    <div id="CartCard" class="row g-0" style="background-color: rgb(221, 221, 221);">
                        <div class="col-xs-4 col-sm-4 col-md-4 col-lg-4 col-xl-4 col-xxl-4">
                            <input type="checkbox" name="CartCheck" checked>
                            <!--Дописать Css поменяв расположение кнопки -->
                            <img id="cardImage" src="{{item.cardImage}}" class="img-fluid rounded-start" alt="=)">
                        </div>
                        <div class="col-xs-8 col-sm-8 col-md-8 col-lg-8 col-xl-8 col-xxl-8">
                            <div class="card-body">
                                <h5 class="card-title">{{item.TovarName}}</h5>
                                <h6 class="card-title">{{item.TovarPrice}}
                                    {{item.currency}}</h6>
                                <a role="button" onclick="delFromFav()">Удалить</a>
                            </div>
                        </div>
                    </div>
                </div>

                {% endfor %}
            </div>
        </div>
        </div>
    </main>
    {% include "templates/footer.html"%}
</body>

</html>